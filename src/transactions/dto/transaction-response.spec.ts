import { TransactionsResponseDto } from "./transactions-response.dto";

describe('TransactionsResponseDto', () => {
  it('should be defined', () => {
    expect(TransactionsResponseDto).toBeDefined();
  });
})